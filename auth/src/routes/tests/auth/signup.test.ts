import {} from '../../../index'
import request from 'supertest';

describe ('auth routes tests', ()=> {
    // it('should get token when signing up', async () => {
    //     await request(app)
    // })
    
    

    it('should get token when signing in', async () => {

    })
})